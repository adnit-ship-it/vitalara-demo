/**
 * Default configuration for intake forms
 */
export const intakeFormConfig = {
  // Default quiz ID to use when no specific product is selected
  // This is typically used when user clicks "Get Started" on homepage
  defaultQuizId: "glp1-weight-loss",
} as const;

